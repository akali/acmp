#include <iostream>
#include <cstdio>
#include <cstdlib>
#include <cstring>
#include <cmath>
#include <string>

using namespace std;

int main () {
	freopen ("input.txt", "r", stdin);
	#ifndef AKALI
		freopen ("output.txt", "w", stdout);
	#endif

	int n, m;
	int a, b;
	cin >> n >> m >> a >> b;

	bool used[100][100];

	int x = 1, y = 1;
	int dx[] = {1,  0, -1, 0};
	int dy[] = {0, -1,  0, 1};
	int k = 0;
	while (x != a && y != b) {
		if (k == 0) {
			if ()
		}
	}	


	return 0;
}
