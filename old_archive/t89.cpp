#include <bits/stdc++.h>

using namespace std;
 
int main(){
 freopen("input.txt","r",stdin);
 freopen("output.txt","w",stdout);
 
 string t,s;
 cin>>N;
  
 for(i=1;i<=N;i++){
   
  t="";ka=0;
  minut=0;
 cin>>g;
  if(x<x1 || (x==x1 && Y<Y1)) {
    minut = (x1*60+Y1)-(x*60+Y);
  }
  else minut=(1440-(x*60+Y))+x1*60+Y1;
  if(mini>minut){mini=minut;s=t;}
}
double MN = mini;
double A = MN / 60;
double speed = 650 / A;
}