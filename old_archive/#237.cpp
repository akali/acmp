#include <cstdio>
#include <cstdlib>
#include <cstring>

using namespace std;

char c[10][100];
char d[111];
int n, m;

int main()
{
 scanf("%d%d", &n, &m);

 for(int i = 0; i < n; i++)
  for(int j = 0; j < n; j++)
   c[i][j] = getchar();
  
 while(m--)
 {
  gets(d);
  int s = 0;
  
  while(s)
 }
}

