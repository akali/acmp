#include <bits/stdc++.h>

int d[90][90], i, j, n;
char s[90];

int main() {
    /*std :: fstream Q ("input.txt"); Q >> s;

    n = strlen(s);

    d[0][0] = 1;
    
    for (; i < n; i++)
        for (j = -1; j++ <= i;)
            d[i + 1][j + 1] += d[i][j] * (s[i] == '?' | s[i] == '('),
            d[i + 1][j - 1] += d[i][j] * (s[i] == '?' | s[i] == ')');
    std :: ofstream ("output.txt") << d[n][0];*/
	std :: cerr << int ('?');
}
