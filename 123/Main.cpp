#include <fstream>
 
int d[90][90], i, j, n;
char s[90];

#define O 1] += d[i][j] * (s[i] == 63 | s[i] ==
                    
main() {
    std :: fstream ("input.txt") >> s;
 
    **d = 1;

    n = strlen (s);
    
    for (; i < n; i++)
        for (j = -1; j++ <= i;)
            d[i + 1][j + O 40),
            d[i + 1][j - O 41);
    
    std :: ofstream ("output.txt") << *d[n];
}